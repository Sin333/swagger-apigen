<% const indent = (s) => s.split('\n').map(l => l.length > 0 ? '    ' + l : l).join('\n'); -%>
import { request as __request } from './fetch-my-custom';
<% if (it.bundledTypeImports.length > 0 || it.typeImports.length > 0) { -%>

import type {
<% for (const name of it.bundledTypeImports) { -%>
    <%= name %>,
<% } -%>
<% for (const name of it.typeImports) { -%>
    <%= name %>,
<% } -%>
} from '<%= it.typesImportFrom %>';
<% } -%>

/** Auto-generated file. Do not edit. */

export class Service {
<% for (let i = 0; i < it.endpoints.length; i++) { -%>
<%~ indent(it.endpoints[i].trimEnd()) %>
<% if (i < it.endpoints.length - 1) { -%>

<% } -%>
<% } -%>
}
